local UserInputService = game:GetService("UserInputService")
local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local UICorner = Instance.new("UICorner")
local TitleBar = Instance.new("Frame")
local CloseBtn = Instance.new("TextButton")
local Sidebar = Instance.new("Frame")
local MainContent = Instance.new("Frame")

-- Cấu hình màu sắc (Đen toàn diện)
local BG_BLACK = Color3.fromRGB(15, 15, 15) -- Màu nền chính siêu tối
local TITLE_BLACK = Color3.fromRGB(25, 25, 25) -- Thanh tiêu đề hơi sáng hơn tí để phân biệt
local TOGGLE_KEY = Enum.KeyCode.RightControl

-- 1. ScreenGui
ScreenGui.Name = "UltraCleanGui"
ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false

-- 2. Khung chính
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = BG_BLACK
MainFrame.Position = UDim2.new(0.5, -250, 0.5, -150)
MainFrame.Size = UDim2.new(0, 500, 0, 300)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true 

UICorner.CornerRadius = UDim.new(0, 10)
UICorner.Parent = MainFrame

-- 3. Thanh tiêu đề (Chỉ giữ lại nút X)
TitleBar.Name = "TitleBar"
TitleBar.Parent = MainFrame
TitleBar.BackgroundColor3 = TITLE_BLACK
TitleBar.Size = UDim2.new(1, 0, 0, 35)
TitleBar.BorderSizePixel = 0

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 10)
TitleCorner.Parent = TitleBar

CloseBtn.Name = "CloseBtn"
CloseBtn.Parent = TitleBar
CloseBtn.BackgroundTransparency = 1
CloseBtn.Position = UDim2.new(1, -35, 0, 0)
CloseBtn.Size = UDim2.new(0, 35, 0, 35)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 14

-- 4. Sidebar rỗng (Vùng bên trái)
Sidebar.Name = "Sidebar"
Sidebar.Parent = MainFrame
Sidebar.BackgroundTransparency = 1 -- Làm trong suốt luôn
Sidebar.Position = UDim2.new(0, 0, 0, 35)
Sidebar.Size = UDim2.new(0, 100, 1, -35)

-- 5. Vùng nội dung rỗng (Vùng bên phải)
MainContent.Name = "MainContent"
MainContent.Parent = MainFrame
MainContent.BackgroundTransparency = 1 -- Làm trong suốt luôn
MainContent.Position = UDim2.new(0, 100, 0, 35)
MainContent.Size = UDim2.new(1, -100, 1, -35)

-- 6. Đóng/Mở
CloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
end)

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if not gameProcessed and input.KeyCode == TOGGLE_KEY then
        MainFrame.Visible = not MainFrame.Visible
    end
end)
